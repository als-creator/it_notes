# 🧙 Emacs. Полный мануал для полного новичка

> **Вы впервые видите Emacs и хотите его освоить?** Тогда этот мануал для вас.
>
> Emacs — это не просто текстовый редактор, это **экосистема и стиль жизни** разработчика, существующая с 1976 года. Его часто называют «операционной системой, которая умеет редактировать текст» — в нём работают почта, календарь, терминал, файловый менеджер, интернет… И да, редактируют код.
>
> 📌 Главное предупреждение: **не пугайтесь поначалу**. Emacs выглядит «древним», но за ним стоит невероятная гибкость и мощь, которой нет ни у одного IDE.

---

## 🤔 Чем Emacs отличается от Vim (для ориентира)

- **Vim** — режимы (Normal/Insert/…), команды по буквам.
- **Emacs** — **без режимов** (по умолчанию): сразу печатаете текст, а команды вызываются комбинациями, где почти всё завязано на **`Ctrl` и `Alt`**.
- В Emacs вы держите руки **на клавиатуре** и обычно **не используете мышь**, если не хотите.

> Принятое обозначение в документации:
> - `C-x` означает **Ctrl+x** (удерживаем Ctrl и жмём x).
> - `M-x` означает **Alt+x** (M — Meta, обычно Alt).
> - `C-x C-s` — нажать Ctrl+x, затем Ctrl+s (последовательно).

---

## 🧭 Дорожная карта для новичка (первые шаги)

1. **Установите Emacs** (ниже).
2. **Запустите**: `emacs` (терминал) или `emacs -nw` (без GUI-окна).
3. **Сразу пройдите встроенный учебник**: нажмите `C-h t` — откроется интерактивный tutorial прямо в редакторе. Это аналог `vimtutor` у Vim. ⭐ обязательный первый шаг.
4. **Выучите минимум команд** (ниже) — их вполне хватит, чтобы начать.
5. Попробуйте себя в **интерактивных тренажёрах** и играх (ссылки ниже).
6. Начните работать и **расширяйте по мере необходимости** — не пытайтесь выучить всё сразу.
7. Когда почувствуете базу — попробуйте **Doom Emacs** (готовая сборка, ссылки ниже), чтобы редактор «просто работал».

> 🎓 Встроенная справка — лучший друг:
> - `C-h ?` — обзор всех видов справки
> - `C-h k <клавиша>` — что делает клавиша
> - `C-h f <команда>` — справка по функции
> - `C-h a <слово>` — поиск команд по слову
> - `M-x <command> RET` — выполнить любую команду (если знаете её имя)

---

## ⚙️ Установка Emacs

### Linux / Arch / EndeavourOS (вы — на CachyOS/EndeavourOS)
```bash
# Классический GNU Emacs
sudo pacman -S emacs

# Версия с pgtk (лучше работает графический режим)
sudo pacman -S emacs-pgtk      # если есть в репозитории

# Doom Emacs потребует git, ripgrep и др.
sudo pacman -S git ripgrep fd
```
### macOS
```bash
brew install --cask emacs        # GUI-версия
```
### Windows
Скачайте установщик с [gnu.org](https://www.gnu.org/software/emacs/download.html).

---

## 🕹️ Интерактивные тренажёры и игры

### 🇷🇺 На русском

| Ресурс | Что это |
|--------|---------|
| [**Викиучебник: Emacs (ru)**](https://ru.wikibooks.org/wiki/Emacs) | Подробный русскоязычный учебник с самого начала. |
| [**Встроенный tutorial (`C-h t`)**](https://www.gnu.org/software/emacs/tour/) | Официальный учебник при первом запуске — есть русский язык (внутри Emacs: `M-x help-with-tutorial`). |
| [**Статьи «Emacs для начинающих» (Habr, на русском)**](https://habr.com/ru/search/?q=emacs%20для%20новичков) | Много пошаговых гайдов на русском. |

### 🌍 На английском (отличные, если язык не мешает)

| Ресурс | Что это |
|--------|---------|
| [**GNU Emacs Tour**](https://www.gnu.org/software/emacs/tour/) | Официальный интерактивный тур по возможностям. |
| [**Emacs Tutor**](https://github.com/emacs-doc/emacs-tutorial) | Встроенный tutorial (в самом Emacs — `C-h t`). |
| [**Prelude и другие «интерактивные» ресурсы**](https://gitlab.com/emacs-meta/emacs-inner-interactive) | Интерактивное изучение. |
| Внутри Emacs **M-x tetris** 🕹️ / **M-x snake** 🐍 | Игры, встроенные прямо в редактор — заодно тренируете комбинации клавиш! |

> 🎯 Совет: играйте во встроенные `tetris`/`snake` — весёлый способ привыкнуть к клавиатурным комбинациям.

---

## 🔑 Минимум команд для старта (первый день)

### Открыть / сохранить / выйти
| Команда | Действие |
|---------|----------|
| `C-x C-f` | **Открыть файл** (Find file) |
| `C-x C-s` | **Сохранить** (Save) |
| `C-x C-w` | Сохранить как |
| `C-x C-c` | **Выйти** из Emacs |
| `C-x k` | Закрыть текущий буфер |
| `C-x b` | Переключить буфер |

### Навигация по тексту
| Команда | Действие |
|---------|----------|
| `C-f` / `C-b` | Вперёд / назад по символам |
| `C-n` / `C-p` | Следующая / предыдущая строка |
| `M-f` / `M-b` | Слово вперёд / назад |
| `M-<` | В начало файла |
| `M->` | В конец файла |
| `C-a` | В начало строки |
| `C-e` | В конец строки |
| `C-v` / `M-v` | Страница вперёд / назад |
| `C-l` | Перерисовать экран и центрировать курсор |

### Удаление и редактирование
| Команда | Действие |
|---------|----------|
| `C-d` | Удалить символ **под** курсором |
| `DEL` (Backspace) | Удалить символ **перед** курсором |
| `M-d` | Удалить слово |
| `C-k` | Удалить до конца строки |
| `M-k` | Удалить до конца предложения |
| `C-@` (или `C-space`) | Начать выделение (mark) |
| `C-w` | Вырезать выделенное |
| `M-w` | Скопировать выделенное |
| `C-y` | Вставить (yank) |
| `C-g` | **Отменить** текущую/ошибочную операцию (очень важно!) |

**Схема работы «вырезать — вставить» (kill-ring):**

```
    C-k (вырезать строку)        C-y (вставить/yank)
    C-w (вырезать выделенное)   ──► последнее вставится
    M-w (скопировать)           M-y — перебрать предыдущие
      ▲                                │
      │   всё, что вырезали, попадает  │
      │   в КОЛЬЦО (стек), не теряется │
      └────────────────────────────────┘
     (kill-ring хранит даже случайно удалённые C-k!)
```

### Отмена и повтор
| Команда | Действие |
|---------|----------|
| `C-/` (или `C-x u`, `C-_`) | **Отменить** последнее действие (Undo) |
| `C-g` | Прервать выполняемую команду |

**Схема «спасательный круг C-g»:**

```
  Вы что-то нажали и Emacs "завис", ждёт продолжения
  (например, вы начали C-x C-f и передумали)…
                  C-g  ← ГОТОВО!
        ┌───────────────────────────────┐
        │  C-g = отмена текущего ввода,  │
        │  возврат в нормальное состояние│
        └───────────────────────────────┘
   C-g НЕ отменяет уже сделанное (это C-/), 
   он просто ПРЕРЫВАЕТ то, что вы начали набирать.
```

> ⚠️ В Emacs «отменить» — это `C-/`, а не `Ctrl+z`. И `C-g` («Control-g») — ваш **спасательный круг**: он отменяет любую наполовину введённую команду.

**Схема «пространства клавиш»** (как устроены все комбинации). Ключи-«префиксы» — это «комнаты» Emacs:

```
 C-x     — "Управление редактором"  (extended)
 ♦ C-x C-f  открыть файл      ♦ C-x C-s  сохранить
 ♦ C-x C-c  выйти             ♦ C-x 2/3  сплит
 ♦ C-x b    переключить буфер ♦ C-x o    другое окно

 C-c     — "Команды программ/пакетов" (mode-specific)
 ♦ C-c C-c  подтвердить/выполнить (в каждом режиме своё)
 ♦ C-c f    например: открыть файл (настраивается)

 C-h     — "Справка" (help)
 ♦ C-h t  tutorial     ♦ C-h k  узнать клавишу
 ♦ C-h f  по функции   ♦ C-h a  поиск по слову

 M-x     — "Войти по имени" (полный доступ ко всему)
 ♦ M-x tetris   ♦ M-x package-install   ♦ M-x <любая команда>
```

> 💡 Любая комбинация Emacs — это просто **ingress «от двери к комнате»**: сначала префикс, потом действие. Не помните хоткей? Нажмите `M-x` и наберите имя команды — Emacs подскажет.

### 🧠 Логика запоминания (очень помогает!)
- `C-` (Ctrl) = «**символ/маленький шаг**»: `C-f` буква, `C-n` строка.
- `M-` (Alt/Meta) = «**слово/большой шаг**»: `M-f` слово, `M-<` в начало.
- Буквы английские и часто логичны: **F**ind, **S**ave, **K**ill, **Y**ank, **D**elete, **N**ext, **P**revious, **U**ndo.

**Схема «двух масштабов» `C-` и `M-`** — вся навигация устроена парой «мелкий/крупный шаг»:

```
   C- (Ctrl) = всё по одному          M- (Alt) = всё по крупному
┌───────────────────────────────┐  ┌─────────────────────────────┐
│ C-f │ C-b   символ вперёд/назад│  │ M-f │ M-b   слово вперёд/назад│
│ C-n │ C-p   строка вниз/вверх │  │ M-< │ M->   начало/конец файла │
│ C-a │ C-e   край строки       │  │ M-v │ C-v   страница           │
│ C-d │ C-k   удалить символ    │  │ M-d │ M-k   удалить слово      │
│     │                        │  │     │        / предложение      │
└───────────────────────────────┘  └─────────────────────────────┘
        ─── по СЛОВАМ вы едете через M- , по БУКВАМ через C- ───
```

**Карта навигации по тексту** (одна картинка — вся суть):

```
                     M-f (слово вперёд)
                    ┌──────────►

 M-< (начало файла)     hello world     M-> (конец файла)
 ────────►             ◄─────           ────────►
                      M-b (слово назад)

        ┌──────────────────────────────┐
        │ C-a ► начало строки          │
        │    hello world  C-e ► конец  │
        │       ▲    ▲                 │
        │   C-b │    │ C-f (буква)     │
        └──────┴────┴──────────────────┘
               C-n (↓) / C-p (↑) — строки
```

---

## 🌈 Система «окон и буферов»

- **Буфер** — это то, что вы редактируете (файл, терминал, справка). Их может быть много, но видим один (или несколько).
- **Окно** — рамка, в которой показывается буфер. Можно делить экран.
- **Фрейм** — то, что вы видите как «окно приложения».

**Схема трёх уровней:**

```
        ФРЕЙМ (frame) — само «окно» приложения на экране
   ┌──────────────────────────────────────────────┐
   │  ОКНО 1 (window)     │  ОКНО 2 (window)       │
   │  буфер "notes.txt"   │  буфер "*scratch*"     │  ← C-x 3 (вертикальный сплит)
   │                       │                       │
   │  ┌────────────────┐  │                       │
   │  │ ОКНО 3          │  │                       │  ← C-x 2 (горизонтальный)
   │  │ буфер "*help*"  │  │                       │
   │  └────────────────┘  │                       │
   ├──────────────────────────────────────────────┤
   │      МИНИБУФЕР (minibuffer) — строка ввода    │  ← тут вводятся C-x C-f и M-x
   └──────────────────────────────────────────────┘

   Буферов (в фоне) может быть 100+: *scratch*, *Messages*, *Help*, все файлы…
   Окна — это просто "витрины", в которых показываются буферы.
```

| Команда | Действие |
|---------|----------|
| `C-x 2` | Разделить экран по горизонтали |
| `C-x 3` | Разделить экран по вертикали |
| `C-x o` | Перейти в следующее окно (very useful!) |
| `C-x 0` | Закрыть текущее окно |
| `C-x 1` | Оставить только текущее окно |
| `C-x b` | Переключить буфер |
| `C-x C-b` | Список буферов |

> 💡 Ещё одна важная вещь — **точка (point)** и **метка (mark)**: `C-SPC`/`C-@` ставит метку, затем команда применяется от точки до метки. Это «выделение» в Emacs (как visual в Vim).

---

## 🧙 Расширение Emacs

Мощь Emacs — в расширяемости. Расширения пишутся на **Emacs Lisp (elisp)** — встроенном языке. Пакеты устанавливаются из **MELPA** (репозиторий пакетов).

### Установка пакета (общий способ)
```emacs-lisp
M-x package-refresh-contents   ; обновить список пакетов (один раз)
M-x package-install RET <имя-пакета> RET
```
Чтобы пакеты имели смысл, добавьте в конфиг:
```emacs-lisp
(require 'package)
(add-to-list 'package-archives '("melpa" . "https://melpa.org/packages/") t)
(package-initialize)
```

### Файл конфигурации
- Конфиг — файл `~/.emacs.d/init.el` (или `~/.emacs`).
- Это просто elisp-код, который исполняется при старте.
- Пример для начала:
```emacs-lisp
;; Номер строки
(global-display-line-numbers-mode 1)
;; Подсветка парных скобок
(electric-pair-mode 1)
;; Своё сочетание: C-c f открывает файл быстрее (тест)
(global-set-key (kbd "C-c f") 'find-file)
```

---

## 🏗️ Готовые сборки Emacs (для быстрого старта)

Точно так же, как Vim имеет LazyVim, у Emacs есть готовые дистрибутивы. Для новичка это лучший способ получить «современный IDE с коробки», не настраивая всё вручную.

| Сборка | Ссылка | Чем выделяется |
|--------|--------|----------------|
| [**Doom Emacs**](https://github.com/doomemacs/doomemacs) | [doomemacs.org](https://doomemacs.org/) | ⭐ **Самая популярная современная сборка.** Быстрая, с плагинами, LSP, красива. Отличный выбор для новичка, который хочет серьёзный редактор. Основана на карте клавиш в стиле Spacemacs с встроенной помощью. |
| [**Spacemacs**](https://github.com/syl20bnr/spacemacs) | [spacemacs.org](http://spacemacs.org/) | Ориентирована на «vim-стиль» (evil режим) и Leader = пробел. Отлична, если вы знаете Vim. |
| [**Prelude**](https://github.com/bbatsov/prelude) | — | Лёгкая, понятная, хороша как первый шаг для самостоятельной настройки. |
| [**Scratch (vanilla)**](https://www.gnu.org/software/emacs/) | — | Чистый Emacs без сборок — чтобы понять основы перед установкой Doom. |

> 💡 Рекомендуемый путь новичка:
> **1)** Чистый Emacs + `C-h t` (освоить базовые команды) →
> **2)** Поиграть в тренажёры →
> **3)** Установить **Doom Emacs** и работать в нём, постепенно разбираясь.

---

## 📚 Полезные ресурсы

- [**Официальный сайт GNU Emacs**](https://www.gnu.org/software/emacs/)
- [**GNU Emacs Manual**](https://www.gnu.org/software/emacs/manual/html_node/emacs/index.html) — основной справочник
- [**Emacs Lisp Intro**](https://www.gnu.org/software/emacs/manual/html_node/eintr/index.html) — введение в elisp
- [**MELPA**](https://melpa.org/) — репозиторий пакетов
- [**EmacsWiki**](https://www.emacswiki.org/) — вики-сообщество
- [**Список замечательных Emacs-пакетов**](https://github.com/emacs-tw/awesome-emacs)

---

## ✅ Чек-лист новичка

- [ ] Установил Emacs
- [ ] Прошёл встроенный tutorial (`C-h t`)
- [ ] Выучил: `C-x C-f` (открыть), `C-x C-s` (сохранить), `C-x C-c` (выйти), `C-g` (прервать), `C-/` (отменить)
- [ ] Научился перемещаться: `C-f/b/n/p`, `M-<`, `M->`
- [ ] Выучил `C-x o` (переключение окон), `C-x 2` (сплит)
- [ ] Сыграл в `M-x tetris` для привыкания к клавишам 😄
- [ ] Установил Doom Emacs (или понял, чем расширять свой)

---

> 🎉 **Итог:** Emacs поначалу пугает, но за 2–3 дня активной практики вы освоитесь с базой. Дальше — бесконечный путь расширения на элиспе. Не пытайтесь охватить всё сразу; главное — встроенный tutorial и постоянная практика. Удачи! 🧙♂️
